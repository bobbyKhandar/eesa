[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-pipeline"
version = "0.1.0"
description = "OCR pipeline module"
requires-python = ">=3.7"

[tool.setuptools]
packages = ["src", "ai_pipeline"]
package-dir = {"ai_pipeline" = "."}
